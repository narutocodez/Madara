const Discord = require('discord.js');module.exports = {name: 'addemoji',execute(client, message, args){if(message.member.hasPermission("ADMINISTRATOR")){if(!args.length) return message.reply("Please specify one or more emojis!");for(const rawEmoji of args) { const parsedEmoji = Discord.Util.parseEmoji(rawEmoji); if(parsedEmoji.id) {const extension = parsedEmoji.animated ? ".gif" : ".png"; const url = `https://cdn.discordapp.com/emojis/${parsedEmoji.id + extension}`; message.guild.emojis.create(url, parsedEmoji.name).then((emoji) => message.channel.send(`Added: \`${emoji.url}\``))}}}},};