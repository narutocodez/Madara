const fetch=require("node-fetch"); module.exports={ name: "meme",async execute(client, message, args, Discord){let data=await fetch(`http://meme-api.herokuapp.com/gimme/memes`).then(res => res.json());const embed=new Discord.MessageEmbed() .setTitle(data.title) .setURL(data.postLink) .setColor("RANDOM") .setFooter(data.ups + " Upvotes") .setTimestamp() .setImage(data.url);message.channel.send(embed)}};