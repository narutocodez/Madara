const db=require("quick.db");const discord=require("discord.js");module.exports={name: "botstatus",usage: "status <here>",category: "owner",ownerOnly: true,async execute(client, message, args){if(message.member.hasPermission("ADMINISTRATOR")){if(!args.length){return message.channel.send("Please put a status argument for the bot")};db.set(`status`, args.join(" "));client.user.setActivity(args.join(" "));message.channel.send("Updated the bot status")}}};