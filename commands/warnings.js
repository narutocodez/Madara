const db=require("quick.db");module.exports={name: "warnings",category: "moderation",async execute(client, message, args){if(message.member.hasPermission("ADMINISTRATOR")){const user=message.mentions.members.first() || message.author;let warnings=db.get(`warnings_${message.guild.id}_${user.id}`);if(warnings === null) warnings=0;message.channel.send(`${user} has/have **${warnings}** warning(s)`)}}};