module.exports={ name: 'unban',async execute(client, message, args, Discord){if(message.member.hasPermission("ADMINISTRATOR")){if (!(message.member.roles.cache.some(r => r.id === "860211377368203315"))) return;let reason;const user=await client.users.fetch(args[0]);if (!args[1]){reason="Not specified"}else{reason = args[1]}const embed=new Discord.MessageEmbed() .setTitle(`${user.id} has been unbanned!`) .setDescription(`${user.username} has been unbanned by ${message.author.username}`) .setColor("RANDOM") .setFooter("UNBANNED") .setTimestamp();message.channel.send(embed);message.guild.members.unban(user);}}};